{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the Ravyn setup and main windows",
  "windows": ["setup", "main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
