# Deep audit log — 2026-07-16

## Objective
Full hands-on QA and refinement of Ravyn: all menus/interactions, UI alignment and context menus, Mica/backdrops across overlays, notification badge, app/Firefox icons, torrent analysis/downloads, YouTube, torrent default association, and compact Firefox popup replacement. No commits or pushes.

## Audit step 1 — environment and baseline
- Recovered objective from the supplied attachment; it matches the user mandate.
- Serena project is active at `C:\Users\tommy\Documents\Ravyn`; reviewed relevant memories: completion gates, synthetic backdrop, Firefox extension, media lifecycle, rqbit contracts.
- Git MCP reports branch `master`, clean worktree, tracking `origin/master`.
- Available integrations confirmed: Serena, Git, SQLite, Tauri MCP, Context7, and Windows Computer Use.
- Computer Use bootstrap attempted. Plugin script exists, but the native pipe connection failed with OS error 2; direct Windows automation is unavailable right now. Tauri MCP remains available for real UI inspection/clicking/logs and will be used.
- Running process inventory shows installed `C:\Users\tommy\AppData\Local\Ravyn\Ravyn.exe` (PID 15256); no repo debug desktop process.
- `rg` is unavailable in the shell, so subsequent file discovery will use PowerShell equivalents.
- No source changes made at baseline.

## Audit step 2 — live driver bootstrap
- Located sample torrents: `C:\Users\tommy\Downloads\AA00FF5861D100E9160C5F0EA18F323EF6563138.torrent` (13,134 B) and requested fixture `C:\Users\tommy\Downloads\Ravyn\Torrents\KNOPPIX_V9.1DVD-2021-01-25-EN.torrent` (90,447 B).
- Built `ravyn-desktop` with debug-only `mcp-automation`; build passed with only Cargo's known bin/lib PDB filename-collision warning.
- Corrected launch target distinction: `target/debug/ravyn.exe` is backend CLI; `target/debug/ravyn-desktop.exe` is the Tauri shell.
- Bridge server connected on 127.0.0.1:9223, backend on an ephemeral loopback port, but setup webview stayed hidden and DOM/log calls timed out.
- Root cause matches `mem:frontend/synthetic_backdrop`: active setup/main capabilities lack `mcp-bridge:default` and `withGlobalTauri` is false.
- Temporary instrumentation patch applied to `src-tauri/tauri.conf.json`, `capabilities/setup.json`, and `capabilities/main.json`. It must be reverted after UI testing; it is not a product/release change.

## Audit step 3 — functional reproduction and first fixes
- Restored all temporary Tauri instrumentation changes; generated schemas returned clean. The live-driver limitation is environmental: both setup and completed-state main windows were created natively but WebView2 stayed `visible:false`, with DOM/log execution timing out. Stopping the installed instance and pinning the dev URL to IPv4 did not change it. No debug-only capability change remains.
- Frontend baseline: `npm.cmd run check` passed with 0 errors/warnings; full Vitest baseline passed 22 files / 108 tests.
- Requested KNOPPIX fixture was submitted directly to managed rqbit 9.0.0-beta.2 with `list_only=true`: HTTP 200, correct info hash, 9 files.
- The same fixture was submitted through an isolated authenticated Ravyn `/v1/torrents/probe`: success with correct files and info hash. Current code does not reproduce `REMOTE_HTTP_ERROR`; the engine/request path is healthy.
- Real defect found: probe returned `total_bytes:null` although rqbit supplied all file lengths. Updated `src/adapters/torrent/wire.rs` to safely derive the total when no explicit aggregate exists; added `derives_probe_total_from_rqbit_file_lengths`. `cargo test --locked --lib derives_probe_total_from_rqbit_file_lengths` passed and `cargo fmt --all -- --check` passed. A prior broader targeted invocation hit a transient MSVC `link.exe` 0xc0000409 while competing builds were winding down; the isolated library test passed immediately afterward.
- Managed yt-dlp 2026.07.04 successfully probed YouTube (`jNQXAC9IVRw`, “Me at the zoo”). Ravyn `/v1/media/probe` also succeeded. A real authenticated media job downloaded format 18, reached `JOB_COMPLETED`, persisted a 629,172-byte MP4 output and SHA-256, then the audit job/file were removed. Current checkout does not reproduce YouTube failure; installed configuration/version remains the likely source.
- UI root cause identified for misplaced three-dot menus: `position:fixed` menu was rendered inside backdrop/transform-containing ancestors and icon-only anchors always used the trigger’s left edge. Updated `Menu.svelte` to use the HTML popover top layer, viewport clamping, end alignment, hidden-until-positioned rendering, and scroll dismissal. Updated `MenuButton.svelte` to right-align icon-only menus. Added end-anchor regression coverage; shared overlay tests pass 4/4.
- Notification counter alignment refined with border-box sizing, explicit flex centering, fixed line height, non-shrinking layout, and tabular numerals in `StatusBar.svelte`.

## Audit step 4 — settings, icons, extension, and Mica
- Torrent default-app registration compared against current HKCU registry and Microsoft Default Programs docs. Existing keys were present, but the command opened only the generic settings page, reused one ProgID for both files and URLs, and never invalidated the Shell association cache.
- Updated `torrent_association.rs`: distinct `Ravyn.Torrent` and `Ravyn.Magnet` ProgIDs, removal of the incorrect `URL Protocol` marker from the file ProgID, `SHChangeNotify(SHCNE_ASSOCCHANGED, SHCNF_IDLIST|SHCNF_FLUSH)`, and `ms-settings:defaultapps?registeredAppUser=Ravyn` deep link. Added Windows binding and URI test. `cargo check --locked -p ravyn-desktop` and targeted test passed.
- Measured icon alpha bounds: desktop assets occupied only 75% of their canvas; Firefox sizes were inconsistent (75–87%). Cropped the canonical SVG viewBox and regenerated all shipped PNG/ICO and Firefox sizes. New alpha bounds are about 92–94% (32px desktop 30×30, 512px 473×473, Firefox 96px 90×90), with consistent optical padding.
- Replaced Firefox sidebar architecture with a compact 400×580 two-view action popup. Overview retains quick download, page actions, summaries, global controls. Resources integrates scanning, previews, search/type/domain/new/size filters, selection, monitoring, per-site cookies, presets/tags, media options, conversion, batch and paused submission. Context menus and Alt+Shift+R open the popup picker; sidebar manifest/command/contracts/build/source/static files were removed.
- Fixed extension build hygiene: build now clears `dist/firefox` before copying, preventing deleted sidebar assets from surviving into packages. Verified built manifest has no `sidebar_action` and distribution has no sidebar path.
- Extension full `npm run check` passed: Prettier, TypeScript, ESLint, Python compile, web-ext (0 errors/warnings/notices), 23 tests, XPI and source package.
- Browser skill was loaded for visual popup QA, but runtime discovery returned no browsers after required troubleshooting. Computer Use was retried under unrestricted permissions; native pipe still absent (OS error 2). No visual browser claim is made.
- Mica gap: only AppShell instantiated/initialized backdrop; setup remained opaque and trusted setup lacked appearance permission. Moved appearance/backdrop to `App.svelte`, allowed read-only appearance for setup, made setup/content/details/drawers/flyouts translucent, reduced flyout/overlay opacity, and moved Menu/Dialog/FilterFlyout into the browser top layer to avoid clipping/offsets. Updated `docs/SYNTHETIC_BACKDROP.md`.
- After Mica/top-layer changes: frontend strict check 0 errors/warnings, 22 test files / 109 tests passed, production build passed. Cargo all-target check passed.

## Audit step 5 — successful native smoke and torrent lifecycle repair
- Re-ran Tauri MCP with an isolated WebView2 profile. The main window was visible and fully controllable; captured real DOM/accessibility snapshots and screenshots at 1100×720.
- Downloads empty state and Fluent/Mica shell rendered coherently. The three-dot menu opened in the HTML top layer at x=873.61..1073.61 directly beneath and right-aligned to its trigger at x=1041.61..1073.61; it remained inside the 1100×720 viewport. This confirms the positioning fix in the real desktop webview.
- Navigated to Torrents and entered the requested KNOPPIX file through the actual Add torrent dialog. This reproduced the user's exact error: `Couldn't analyze this torrent` with `REMOTE_HTTP_ERROR` against `http://127.0.0.1:3030/torrents?list_only=true`.
- Root cause: the installed managed rqbit component was not started unless the original setup profile included torrent support. Ravyn exposes the torrent UI and allows components to be installed later, so setup-time feature selections were stale runtime gating.
- Changed `Ravyn::bootstrap` to start any checksum-verified active managed rqbit binary regardless of the historical setup selection; custom executable paths remain externally owned.
- Validation used an isolated data directory with the verified rqbit 9.0.0-beta.2 activation metadata/binary. A stale audit rqbit process occupying BitTorrent port 4240 initially caused an expected startup conflict; after removing that audit-only process, the desktop spawned its managed rqbit on an ephemeral loopback HTTP port and the Torrents page reported `Ready — engine ready`.
- Re-entered the exact KNOPPIX fixture in the real Tauri dialog. Probe completed successfully: `Source analyzed`, name `KNOPPIX_V9.1DVD-2021-01-25-EN`, `4.4 GB · 9 files`, all nine selectable, and Add torrent enabled. Screenshot: `target/audit-torrent-probe-fixed.png`.
- `cargo fmt --all` and `cargo check --locked --all-targets` passed after the lifecycle change.
- SQLite MCP confirmed the repository dev DB has no feature-selection row; read-only inspection of the installed DB showed profile `minimal` with only standard downloads while a verified rqbit activation exists, matching the stale-gate reproduction.
- Temporary MCP bridge/global-Tauri instrumentation is still active solely for the remaining smoke pass and must be reverted before release verification.

## Audit step 6 — installed-state YouTube diagnosis and full desktop traversal
- Completed a native traversal of Downloads, Library (Files/Trash/Duplicates), Media (Downloads/Archive and add dialog), Torrents, Automation (Rules/Schedules), Batch queue, notifications, and all 11 Settings categories. All Settings categories rendered at 1100×720 without horizontal overflow.
- Resized the real main window to its supported minimum 800×560. Downloads, Library, Media, Torrents, Automation, and Settings had no document/main horizontal overflow; the navigation collapsed to its icon rail and settings categories remained reachable through their intentional horizontal strip.
- The unread notification counter was exercised with one real completion notification. Its glyph was optically centered in the status bar in `target/audit-youtube-completed.png`.
- Read-only inspection of the installed Ravyn database found the exact user YouTube failure. Job `cd2d69f5-b842-432d-9d49-9f2583f5479e` for `ZmD8BK7aD40` failed with: `Postprocessing: ffmpeg not found. Please install or provide the path using --ffmpeg-location`. The Downloads folder contained the separate `.f251.webm` and `.f399.mp4` fragments, proving download succeeded but merge failed.
- Root cause: managed FFmpeg is stored in Ravyn's private verified engine directory, but yt-dlp was not given that path and the directory is intentionally absent from global PATH.
- Added `append_ffmpeg_location`: non-default/configured FFmpeg paths are passed to yt-dlp with `--ffmpeg-location`; the plain default command remains PATH-discovered. Added regression tests for both cases; both targeted tests passed.
- Rebuilt the MCP desktop, copied verified yt-dlp/FFmpeg activations into the isolated QA data directory, cleared only the QA archive entry, and repeated the complete UI flow. Probe returned “Me at the zoo” with formats; Add media created a job that completed with 252,182 transferred bytes and a registered ready output `Me at the zoo [jNQXAC9IVRw].mkv` (475,425 bytes). Screenshot: `target/audit-youtube-download-fixed.png`.
- Updated component/setup documentation to record current-state rqbit activation and explicit managed FFmpeg handoff.
- Removed all temporary MCP permissions, global Tauri exposure, and IPv4 dev-URL instrumentation. Default desktop check regenerated schemas; only the intentional setup appearance permission remains.

## Audit step 7 — release gates and final hygiene
- Added a guard so managed rqbit only owns Ravyn's built-in `http://127.0.0.1:3030` endpoint; custom/remote endpoints remain operator-owned. Added `managed_rqbit_only_owns_the_builtin_endpoint`.
- Final Rust gates: `cargo fmt --all -- --check` passed; `cargo clippy --locked --all-targets --all-features -- -D warnings` passed; `cargo test --locked --all-targets` passed with 245 library tests plus 11 HTTP integration tests and bench smoke; dedicated `http_integration` passed 11/11; optimized `cargo build --locked --release` passed after the last source change.
- Final frontend gates: strict `svelte-check` passed with 0 errors/warnings; Vitest passed 22 files / 109 tests; Vite production build passed.
- Firefox extension full check had already passed after its final source changes: Prettier, TypeScript, ESLint, Python compile, web-ext 0 errors/warnings/notices, 23 tests, XPI and source package.
- `git diff --check` passed. No temporary MCP bridge permission, global Tauri exposure, IPv4 dev URL, debug Ravyn process, audit rqbit process, or Vite dev process remains.
- Computer Use native pipe and browser runtime remained unavailable; the real application was instead controlled through Tauri MCP with screenshots, accessibility trees, JavaScript geometry/overflow measurements, and actual authenticated backend operations.
- No commit or push was performed.

## Audit step 8 — clean setup/restart QA and repairs
- Computer Use was bootstrapped again for the continuation turn, but the native Windows pipe still returned OS error 2. Continued with real Tauri MCP control.
- Ran a completely clean isolated setup profile through Welcome/Repair, setup type, features, library, preferences, install, restart-required, completion, and main-window startup. No user installation or library was modified.
- Found a development-mode inconsistency: “Create a Start Menu shortcut” was checked while disabled even though the mode explicitly runs in place. Root cause was the controller's default `startMenuShortcut=true` surviving detection of development mode.
- Found nondeterministic restart recovery: after the required backend restart, setup returned to Installation Preferences and required a second Install click. Root cause was `init()` always restoring any prepared library to `preferences`, ignoring persisted integration consent/installation state.
- Added `restoredSetupStep`, which restores prepared-but-uninstalled setup to Preferences and resumes persisted installation at Install. When restart is complete, initialization automatically reruns the idempotent installation/provisioning reconciliation.
- Updated development-mode selection to clear Start Menu, desktop, and startup integration choices both on initial detection and persisted-consent restoration.
- Added four controller tests covering install-stage restoration, preferences restoration, incomplete-library behavior, and development integration reset. Strict Svelte check passed with 0 errors/warnings; targeted controller tests passed 4/4.
- Replayed a second clean profile. Development integration choices were correctly unchecked on first render. After the restart boundary, the next process opened directly at “Installing Ravyn” with the persisted integration row and enabled Continue; screenshot: `target/audit-setup-resume-fixed-r3.png`.
- Completed setup/main startup was verified by relaunching the same isolated data directory after the debug-only MCP bridge's expected one-port handoff conflict; the completed profile opened the real main window. The conflict is specific to two overlapping MCP-enabled debug processes and is absent from release builds.

## Audit step 9 — secondary interaction and diagnostics repairs
- Exercised completed-job details (Overview, Files, Activity, Advanced), row selection/double-click, destructive Remove confirmation/cancel, General preset/profile dialogs, Downloads tuning, Storage move preflight, Tools, Updates, Privacy secret dialog, Firefox integration, and Troubleshooting.
- Found transient Filter and Sort popovers could remain logically open together. Switched Menu/FilterFlyout to auto popovers and synchronized native toggle-close events back into Svelte state. Real webview inspection confirmed only Sort remained expanded and one menu existed. Shared component tests pass 4/4.
- Found component verification checked rqbit at persisted 127.0.0.1:3030 even though managed rqbit is substituted to an ephemeral runtime endpoint. Updated the verify route to use runtime config; rebuild/live recheck pending.
- Found completed media rows could retain the generic URL filename (for example, watch) despite a registered media output. Added safe completed-output filename inference and unit coverage; compile/runtime recheck pending.
- Found Troubleshooting continuously reissued readiness calls because its mount effect tracked the loading state it mutated. Isolated the initial call with Svelte untrack. Live webview changed from permanently Checking to Everything is working; strict Svelte check passed.
- Fixed Svelte binding_property_non_reactive warnings by making Tabs element references rune-reactive. Strict Svelte check passed.
- Targeted frontend regression result: controller + shared components, 8/8 tests passed.

## Audit step 10 — rebuilt smoke, popup accessibility, and clean gates
- Corrected the component verification implementation after compile feedback: ApiState has no direct runtime config, so verification now reads the active config owned by JobManager. All-target check passed.
- Rebuilt the MCP-enabled desktop and repeated Tools > rqbit Verify. The action completed against the supervised ephemeral endpoint, removed the stale 127.0.0.1:3030 error, and updated both Last checked and Last verified. Screenshot: target/audit-rqbit-verify-fixed.png.
- Reopened completed-job details after rebuilding. No Svelte console warnings were emitted. Programmatic focus plus a real ArrowRight key moved Overview to Files and updated focus/aria-selected, confirming Tabs keyboard behavior.
- Rechecked the Add menu and Add download dialog: menu/menuitem semantics are present, initial dialog focus is inside the modal, Escape closes it, and focus returns to Add.
- Browser control runtime initialized successfully but reported zero available browsers after the mandated troubleshooting/discovery pass. No Firefox visual screenshot is claimed. Static popup inspection found and fixed missing standard Arrow/Home/End keyboard navigation and roving tabindex for Overview/Page resources.
- Full Firefox gate passed after the keyboard fix: formatting, TypeScript, ESLint, Python compile, web-ext 0 errors/warnings/notices, 23 tests, XPI, and source package.
- Full frontend gate passed: strict Svelte check 0 errors/warnings, 23 files / 113 tests, production build.
- Full Rust gate passed: fmt check, clippy all targets/features with warnings denied, 247 all-target tests, 11 dedicated HTTP integration tests, bench smoke, optimized release build, and default ravyn-desktop check.
- Updated setup capability documentation with deterministic idempotent restart recovery and development-mode integration behavior.
- Reverted all temporary automation capabilities/global Tauri exposure/IPv4 dev URL, stopped debug Ravyn/rqbit/Vite processes, and confirmed no matching process remained.
- git diff --check passed. No commit or push was performed.

## Audit step 11 — completion-evidence audit and additional interaction defects
- Re-read the original mandate and treated all previous completion claims as unproven. Computer Use still cannot connect because the native pipe is absent (OS error 2); continued with the actual isolated Tauri desktop.
- Built an explicit evidence matrix around direct interaction, UI/Mica/alignment/icons, torrents, YouTube, popup replacement, torrent associations, and Serena logging. Weak evidence remains for direct taskbar/Firefox rendering because the Windows/browser automation surfaces are unavailable.
- Found all icon-only MenuButton triggers omitted aria-label even though every call site supplied a contextual label. Added aria-label for icon-only mode. Live accessibility output now names global More, selection actions, and per-job actions; added regression coverage.
- Exercised real automation CRUD: created a rule, disabled it, reopened prefilled edit state, ran rule preview, and deleted it through confirmation. Created an hourly schedule, ran it immediately, inspected execution history/details, and deleted it through confirmation.
- Found a shipped Batch queue blocker: AppShell hides BasketView's PageHeader, but Start and Clear existed only in that hidden header. Users could populate/edit the basket but could not start or clear it from the actual drawer.
- Added an embedded BasketView mode used by AppShell. The visible drawer toolbar now exposes Clear, Add, and Start while standalone rendering retains its page-header actions without duplicates. Live 1100px and 800px passes confirmed no overflow; real Add, edit, clear-confirm/cancel, start, and clear-confirm/commit paths succeeded.
- Added BasketView regression coverage that boots with a real basket item and verifies embedded Start/Clear/Add plus startBasket/list refresh calls. Basket + shared component targeted suite passed 6/6.
- Exercised Settings preset/profile CRUD and activation, notification mark-all-read/clear-history, failed job retry/removal confirmations, and unsaved-settings navigation cancel/discard.
- Invoked the torrent default-app UI in the isolated development build after exporting current registry keys. The UI correctly refused to register the non-installed debug executable and surfaced 'install Ravyn before registering it'; the registry remained unchanged. Source/runtime verification on an installed new build is still not available without replacing the installed application.
- Added a happy-dom popup test that boots the real popup HTML with mocked Firefox APIs and verifies connection initialization, panel visibility, roving tabindex, focus, ArrowRight, and Home behavior. Targeted popup tests passed 2/2 without network noise.
- Finalized embedded BasketView to avoid duplicate standalone actions: AppShell passes embedded, the drawer receives the visible action toolbar, and standalone BasketView retains PageHeader actions. Live HMR recheck confirmed the empty drawer shows Add and disabled Start basket.
- Full frontend gate passed after the second-round fixes: strict Svelte check 0 errors/warnings, 24 files / 115 tests, and production build.
- Full extension gate passed: formatting, TypeScript, ESLint, Python compile, web-ext 0 errors/warnings/notices, 9 files / 25 tests, XPI, and source archive.
- Default non-MCP ravyn-desktop check passed and regenerated release schemas.
- Final hygiene: temporary MCP permissions/global Tauri/IPv4 dev URL removed; debug Ravyn, audit rqbit, and Vite processes stopped; git diff --check passed; no commit or push.






